# Кейс «Дизайн-система логистической компании» — Fortis | Monopoly

`cases/fortis-ds.html` (RU, готов) · `en/cases/fortis-ds.html` (EN, заглушка)

Общая система страниц кейсов — в README, раздел «Что уже готово» → «Все 4
страницы кейсов». Здесь только то, что специфично для этой страницы.

## Идентификация
- id: `fortis-ds`
- Company · segment · platform: Fortis | Monopoly · b2b · web · app
- Самый длинный по секциям кейс наравне с CRM (6 секций)

## Переменные (на `<main class="case-page" style="...">`)
- `--case-accent: #EEF0F7`
- `--case-tint-bg: #EEF0F7`
- `--case-dark-bg: url(/images/ru/cases/fortis-ds/stats-bg.svg)`
- `--case-hero-bg: #EEF0F7`
- `--case-hero-ratio: 4320/1929`, `--case-hero-ratio-mobile: 1179/621`

## Структура

**Hero** — вариант с KPI (`.case-hero__callout` + `.case-hero__stats`: «5
продуктов», «30+ компонентов»). H1 «Дизайн-система<br>логистической компании».

**01 Контекст и проблема** — A: «Контекст» + текст. B: «Проблема» + текст +
список проблем Material UI. Медиа: `.case-shot` — экраны на старой дизайн-системе
(`context-old-ui.webp`, мобильный кроп — `context-old-ui-mobile.webp`).

**02 Задача и команда** — A: «Задача». B: «Команда». Без медиа.

**03 Мой вклад** — A: «Основной вызов» (`--row1`). B: «Ключевые действия» +
список (`--row1`). `.case-section__anchor--bottom` → `#results` (секция 06).

**04 Решение** — медиа СНАЧАЛА: `.case-flow` — архитектура системы
(`solution-architecture.svg`, мобильный кроп — отдельный, более высокого
разрешения файл `solution-architecture-mobile.svg`). Затем A: «Прошлый опыт»
+ список. B: «UI-kit» + список. Дальше `.case-section__text-group`
«Организация файлов UI-kit'а» с `.case-flow` внутри (`solution-file-structure.svg`
на десктопе). Затем **два парных блока картинка+текст** через
`.case-section__text-group-columns`: «Спецификации» (`.case-shot`
`solution-spec-template.webp` + текст) и «Редактура» (`.case-shot`
`solution-editorial-rules.webp` + текст, обёрнута в доп. класс
`.case-solution-editorial` — только на мобиле увеличивает отступ до блока
«Спецификации» до 48px, точечно, чтобы не тронуть другие такие же пары на
странице).

Мобильный кроп «Организации файлов» — **не svg**, а `solution-file-structure-mobile.webp`
(растровая картинка, формат отличается от десктопной версии — так и должно
быть, замену свели на мобильном источнике `<picture><source>`, десктопный
`.svg` не трогали). Эта картинка уже содержит заголовок «Организация файлов
UI-kit'а» нарисованным внутри себя, поэтому HTML-заголовок `<h3
class="case-section__group-heading">` рядом с ней получил доп. класс
`case-section__group-heading--mobile-hidden` (`display:none` только на
мобиле, в `case.css`) — иначе заголовок дублировался бы. На десктопе
HTML-заголовок остаётся видимым как обычно.

**05 Внедрение** — медиа сначала: `.case-flow` (`rollout-approaches.svg`,
мобильный кроп — `rollout-approaches-mobile.svg`, чистый вектор без
встроенного растра, в отличие от прежней версии).
Затем два `.case-section__text-group`: «Подходы» (текст+текст) и «Выбранная
стратегия» (текст+текст, `--group`).

**06 Результаты** (`id="results"`) — A: «Итог» + список. B: «Дальнейшее
развитие» + список. Медиа: `.case-results__gallery` (см. «Уникальные
компоненты») — 4 экрана: `--full` (`results-uikit.webp`), `--wide`
(`results-search-screen.webp`), `--narrow` (`results-mobile-pair.webp`),
`--wide` (`results-onboarding.webp`).

**Тёмный блок (итоги)** — `.case-stats` (обычная, НЕ `--triple`), 4 карточки
2×2: инициировала создание компонентов и вела беклог · создали 30+ компонентов
· снизили количество повторных обсуждений · упростили взаимодействие дизайна
и разработки.

**Футер** — `{{> case-footer currentCaseId="fortis-ds" }}`.

## Уникальные компоненты этой страницы

### `.case-results__gallery` — экраны на подложке-сетке (06)
Единственный кейс с таким паттерном результатов. В отличие от обычных
`.case-section__media` (картинки прижаты к левому краю текстовых колонок,
5/12–12/12), тут экраны **центрируются** и не тянутся до правого края —
flex-колонка со своей подложкой, а не набор `.case-shot` впритык. Подложка —
лиловая заливка (`--case-tint-bg`) с пунктирной SVG-сеткой поверх, пробита в
100vw (тот же приём отрицательных полей, что у hero-картинки). Модификаторы
ширины экрана: `--full`/`--wide`/`--narrow` (см. `case.css`, раздел «Результаты:
экраны на подложке-сетке»). Анимации подложки при скролле сейчас нет —
обсуждали, но не делали (см. README).

### Парные блоки картинка+текст (04)
`.case-section__text-group-columns` изначально задумывался для пары текст+текст
(см. «Кабинетное» в CRM) — здесь первая колонка пары стала картинкой
(`.case-shot`), а не текстом («Спецификации», «Редактура»). Технически это
тот же компонент с доп. правилом `> .case-shot{ flex:1 }` в `case.css` — если
понадобится третья такая пара на этой или другой странице, паттерн уже готов.

## Анимации на этой странице
| Где | Элемент/класс | Эффект | Файл |
|---|---|---|---|
| Hero | `h1.reveal`, `.case-hero__callout.reveal--fade`, `.case-hero__media.reveal--fade` | появление при скролле (база) | `animations.css` + `reveal-on-scroll.js` |
| 06 | `.case-results__gallery` (пунктирная подложка) | **не анимирована** — статичный фон | `case.css` |
| Тёмный блок | `.case-stats__item.reveal--up[data-delay="0..3"]` ×4 | появление снизу, шаг 0.08s | `animations.css` |
| Тёмный блок | `.case-stats__item::after` | один раз, блик-шиммер по стеклу карточки | `case.css` (`@keyframes case-stats-shimmer`) |
| Тёмный блок | `.case-stats__icon path` | один раз, галочка дорисовывается | `case.css` (`@keyframes case-stats-check`) |
| Футер «Другие кейсы» | `.other-case__cover` (обложки 3 других кейсов) | курсор-параллакс при hover (десктоп) | `case-parallax.js` |

Подложка `.case-results__gallery` (06) — явно упомянутый в коде кандидат на
будущую анимацию прокрутки (см. комментарий в `case.css` про «Результаты»),
пока без реализации.
